/** Page-count choices for a crawl. More pages = deeper audit, higher cost. */
export const CRAWL_PAGE_OPTIONS = [10, 20, 50, 100] as const;

export const DEFAULT_CRAWL_PAGES = 20;

export const EXAMPLE_DOMAINS = ["vercel.com", "stripe.com", "ahrefs.com", "notion.so"];
